NER — sponsor logos (final)
===========================
Unzip at repo ROOT (overwrites). Self-contained: includes logos + data + page.

ADDS:   public/images/sponsors/*.png  (24 white logos, web-safe names)
OVERWRITES:
  src/data/sponsors.ts    - 24 sponsors with confirmed names + URLs
  src/pages/Sponsors.tsx  - featured 3 (logo + blurb) then a logo WALL

Name fixes applied: westside = WS Finishing, did = Daido, diecutting = NEDC.
URLs verified/added: Calspan, Gene Haas Foundation, OshCut, NK Labs, Schroth
  (plus WS Finishing, Daido, NEDC and the previously-known ones).

All 24 sponsors are now linked.

Diamondback Designs: no logo provided, so it is not on the page. Send a logo to add it.

After unzip:  npm run build   then   npm run dev   ->  /sponsors
